async function register ({
  registerHook,
  registerSetting,
  settingsManager,
  storageManager,
  videoCategoryManager,
  videoLicenceManager,
  videoLanguageManager
}) {

}

async function unregister () {
  return
}

module.exports = {
  register,
  unregister
}
